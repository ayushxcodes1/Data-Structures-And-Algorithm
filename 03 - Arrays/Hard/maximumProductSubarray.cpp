#include <bits/stdc++.h>

using namespace std;
 
vector<int> maxProduct(vector<int> arr) {
    
}

int main() {
  vector<int> arr = {2, 3, -2, 4};
  vector<int> ans = maxProduct(arr);
  for(auto it : ans) {
    cout << it << " ";
  }

  return 0;
}