#include <bits/stdc++.h>

using namespace std;

//Brute Force 
int majorityElement(vector<int> arr) {
    int n = arr.size();

}

int main() {
  vector<int> arr = {2, 2, 1, 1, 1, 2, 2};
  int res = majorityElement(arr);
  cout << res;
  return 0;
}