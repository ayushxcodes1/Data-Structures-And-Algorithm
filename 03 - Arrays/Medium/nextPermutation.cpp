#include <bits/stdc++.h>

using namespace std;

vector<int> nextPermutation(vector<int> arr) {

}

int main() {
  vector<int> arr = {3, 2, 1};
  vector<int> ans = nextPermutation(arr);
  cout << ans;
  return 0;
}