#include <bits/stdc++.h>

using namespace std;

int longestConsecutiveSequence(vector<int> &arr) {
    int n = arr.size();
    int longest = 1;
    for(int i = 0; i < n; i++) {
        
    }
}

int main() {
  vector<int> arr = {102, 4, 100, 1, 101, 3, 2, 1, 1};

  return 0;
}