#include <bits/stdc++.h>

using namespace std;

int singleNumber(vector<int> &arr) {
    int n = arr.size();
    int low = 0, high = n - 1;
    while(low <= high) {
        
    }
}

int main() {
  vector<int> arr = {1, 1, 2, 3, 3, 4, 4, 8, 8};
  return 0;
}