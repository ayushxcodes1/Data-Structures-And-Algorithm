#include <bits/stdc++.h>

using namespace std;

int allocateBooks(vector<int> &arr, int k) {
  int n = arr.size();
 
}

int main() {
  vector<int> arr = {12, 34, 67, 90};
  int k = 2;
  
  int ans = allocateBooks(arr, k);
  cout << ans << "\n";
  return 0;
}