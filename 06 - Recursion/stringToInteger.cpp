#include <bits/stdc++.h>

using namespace std;

int myAtoi(string s) {
    int n = s.size();
    
    while(string s, int i)
}

int main() {
  string s = "-42"; 
  int ans = myAtoi(s, 0);
  cout << ans << endl;
  return 0;
}