#include <bits/stdc++.h>

using namespace std;

int myAtoi(string s, int i = 0) {
    int n = s.size();
    
    //skip leading whitespaces
    while(i) 
}

int main() {
  string s = "-42"; 
  int ans = myAtoi(s);
  cout << ans << endl;
  return 0;
}