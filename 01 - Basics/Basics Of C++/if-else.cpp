#include <iostream>

using namespace std;

int main() {
    int age;
    cin >> age;
    if(age >= 18) {
       cout << "eligible for driving license.";
    }
    else {
       cout << "eligible for driving license.";
    }
    return 0;
}