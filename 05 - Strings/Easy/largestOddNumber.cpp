#include <bits/stdc++.h>

using namespace std;

//Brute - TC(N), SC: O(N)
string largestOddNumber(string s) {
    
}

int main() {
  string s = "";

  string ans = largestOddNumber(s);
  cout << ans;

  return 0;
}