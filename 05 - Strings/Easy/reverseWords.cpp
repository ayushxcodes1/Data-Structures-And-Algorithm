#include <bits/stdc++.h>

using namespace std;

string reverseWords(string s) {
    stack<string> st;

    for(auto c : s) {
        if(c == '' ') continue;
    }
}

int main() {
  string s = "  hello  world  ";
  int n = s.length();
  return 0;
}