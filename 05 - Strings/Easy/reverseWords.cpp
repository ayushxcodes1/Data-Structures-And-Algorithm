#include <bits/stdc++.h>

using namespace std;

//Brute Force - TC: O(N), SC: O(N)
string reverseWords(string s) {
    
}

int main() {
  string s = "  hello  world  ";

  string ans = reverseWords(s);
  cout << ans << endl;
  return 0;
}