#include <bits/stdc++.h>

using namespace std;

string longestCommonPrefix(vector<int> &str) {
    int n = str.size();
    string word = "";
    string ans = "";
    int i = 0;
    while(i < n) {
        
    }
}    

int main() {
  vector<string> str = {"flower", "flow", "flight"};
  return 0;
}