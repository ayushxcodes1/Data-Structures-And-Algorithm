#include <bits/stdc++.h>

using namespace std;

string longestCommonPrefix(vector<int> &str) {
    int n = str.size();
    for(int i = 0; i < n; i++) {

    }
}    

int main() {
  vector<string> str = {"flower", "flow", "flight"};
  return 0;
}