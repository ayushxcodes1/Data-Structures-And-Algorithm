#include <bits/stdc++.h>

using namespace std;

string longestCommonPrefix(vector<int> &s) {
    int n = s.size();
    string word = "";
    string ans = "";
    int i = 0;
    while(i < n) {
        word += s[i];
    }
}    

int main() {
  vector<string> s = {"flower", "flow", "flight"};
  return 0;
}