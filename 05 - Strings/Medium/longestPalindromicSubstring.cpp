#include <bits/stdc++.h>

using namespace std;

string longestSubstring(string s) {
    int n = s.length();
    string ans = "";
}

int main() {
  string s = "babad";
  string ans = longestSubstring(s);
  return 0;
}