#include <bits/stdc++.h>

using namespace std;

int countSubstrings(string s) {
    int n = string.length();

    
}

int main() {
  string s = "abcabc";
  int ans = countSubstrings(string s);
  return 0;
}